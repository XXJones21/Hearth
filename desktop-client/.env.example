# Valar is the single client entry point (:8700). These defaults are also
# baked into src/lib/config.ts -- you only need a .env.local when Valar is
# not on this machine (e.g. a tailnet hostname).
VITE_HEARTH_WS=ws://127.0.0.1:8700
VITE_HEARTH_HTTP_ORIGIN=http://127.0.0.1:8700

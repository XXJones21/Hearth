# Valar gateway — clean v1. All open-source.
# Core gateway (the single client entry point).
fastapi>=0.110
uvicorn[standard]>=0.27
httpx>=0.27          # streaming brain-provider HTTP client (SSE)
websockets>=12.0     # uvicorn websocket support

# Server-side STT fork (stt:"server"). Only needed when serving voice clients
# that stream PCM; clients using stt:"local" (client_transcription) don't need it.
openai-whisper>=20231117
numpy>=1.24

# TTS (NeuTTS-Air) is REUSED from the existing Server/ package
# (Server.tools.tts_generator). Its heavy deps — neutts-air, soundfile,
# librosa, torch, llama-cpp-python — are installed with the existing Server,
# not re-pinned here. Valar imports that package via an adapter (valar/voice/tts.py).

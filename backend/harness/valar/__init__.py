"""Valar — the dedicated Valinor harness + single client entry point (clean v1).

Body tier: the universal voice protocol gateway, the BrainProvider seam (default
backend = the existing Rust llama.cpp endpoint), the clean voice loop, the
minimal persona engine, Engram memory recall, and per-turn telemetry. Built
fresh, free of the obsolete weak-27B-era band-aids.
"""

__version__ = "1.0.0"

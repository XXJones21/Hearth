//
//  HearthCore.swift
//
//  A placeholder so the package has a public symbol before area 1 lands the
//  real one. The three targets import HearthCore; this is what proves the
//  product dependency resolves in each of them.
//

/// Replaced in area 1 by ServerConfig, the wire types and the brand layer.
public enum HearthCore {
    public static let name = "HearthCore"
}

# Style reference packs

Reserved for future Microsoft and Meta style supplements.

When a pack is added here, name it clearly (for example `microsoft.md`, `meta.md`) and instruct agents to read it only when the task or dispatch prompt selects that pack. Keep the core rules in `SKILL.md`.

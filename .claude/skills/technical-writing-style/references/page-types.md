# Page types (retired)

This pack moved into the repository and is now published.

Read **`wiki/page-types.md`** instead. It is canonical, it is linted like every other
page, and it survives a clean of this gitignored directory.

Do not restore content here. Two copies of a style rule is the failure this
pipeline exists to find: see `scripts/doc_graph.py`.

Loaded through `.claude/skills/hearth-style/SKILL.md`.
